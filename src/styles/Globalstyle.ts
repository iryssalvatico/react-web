import {createGlobalStyle} from "styled-components"

export default createGlobalStyle`
  
  body {
    width: 100%;
    max-width: 1024px;
    margin: 0 auto;
  }
  h1{
    color: #E6B8B8;
  }
  body{
    width:100;
    max-width: 1400px;
    margin: 0 auto;
  }
`

export const colors = {
    white: "#ffffff",
    fundo: "#E6B8B8"
}